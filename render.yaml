services:
  - type: web
    name: industry-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        fromParam: PORT
      - key: PYTHON_VERSION
        value: "3.12.0"
